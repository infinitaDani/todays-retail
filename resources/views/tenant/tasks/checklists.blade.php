<x-layouts.tenant title="Checklists" subtitle="Plantillas operativas asociadas a turnos"><div class="row g-3 mb-4">@foreach ([['Total', $summary['total'], 'clipboard-list', 'primary'], ['Activos', $summary['active'], 'check-circle-2', 'success'], ['Inactivos', $summary['inactive'], 'pause-circle', 'warning'], ['Con historial', $summary['used'], 'history', 'primary']] as [$label, $value, $icon, $color])<div class="col-6 col-xl-3"><div class="summary-card"><span class="summary-icon bg-{{ $color }}-subtle text-{{ $color }}"><i data-lucide="{{ $icon }}"></i></span><div class="summary-value">{{ $value }}</div><div class="summary-label">{{ $label }}</div></div></div>@endforeach</div><div class="tr-card p-0 overflow-hidden"><form class="listing-toolbar p-3 border-bottom" method="GET"><div class="listing-search input-group"><span class="input-group-text bg-transparent"><i data-lucide="search"></i></span><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Buscar checklist"></div><div class="listing-filters"><select class="form-select" name="shift_id" onchange="this.form.submit()"><option value="">Todos los turnos</option>@foreach($shifts as $shift)<option value="{{ $shift->id }}" @selected((string) request('shift_id') === (string) $shift->id)>{{ $shift->name }}</option>@endforeach</select><select class="form-select" name="status" onchange="this.form.submit()"><option value="">Todos los estados</option><option value="active" @selected(request('status') === 'active')>Activos</option><option value="inactive" @selected(request('status') === 'inactive')>Inactivos</option></select><select class="form-select" name="usage" onchange="this.form.submit()"><option value="">Todo uso</option><option value="used" @selected(request('usage') === 'used')>Con historial</option><option value="unused" @selected(request('usage') === 'unused')>Sin historial</option></select><a class="btn btn-outline-secondary" href="{{ route('checklists.index') }}">Limpiar</a><button class="btn btn-primary" type="button" disabled title="El formulario de creación se incorpora con el CRUD funcional."><i data-lucide="plus" class="me-1"></i>Nuevo checklist</button></div></form><div class="table-responsive"><table class="table table-custom align-middle mb-0"><thead><tr><th>Checklist</th><th>Turno</th><th>Tareas</th><th>Actualizado</th><th>Estado</th><th class="text-end">Acciones</th></tr></thead><tbody>@forelse($checklists as $checklist)<tr><td class="fw-semibold">{{ $checklist->name }}</td><td>{{ $checklist->shift->name }}</td><td>{{ $checklist->items_count }}</td><td>{{ $checklist->updated_at->format('d/m/Y') }}</td><td><span class="badge badge-label badge-soft-{{ $checklist->status === 'active' ? 'success' : 'warning' }}">{{ $checklist->status === 'active' ? 'Activo' : 'Inactivo' }}</span></td><td class="text-end"><span class="text-muted small">Sin acciones disponibles</span></td></tr>@empty<tr><td colspan="6"><div class="listing-empty">No se encontraron checklists.</div></td></tr>@endforelse</tbody></table></div><div class="listing-pagination px-3">{{ $checklists->links() }}</div></div></x-layouts.tenant>
