<x-layouts.tenant><h1>Knowledge Center</h1><form method="post">@csrf<input name="title" placeholder="Título" required><input name="category" placeholder="Categoría"><input name="version" type="number" value="1" min="1"><select name="status"><option value="draft">Borrador</option><option value="published">Publicado</option><option value="inactive">Inactivo</option></select><textarea name="content" placeholder="Contenido" required></textarea><button>Crear artículo</button></form>@foreach($articles as $article)<section><h2>{{ $article->title }} v{{ $article->version }} ({{ $article->status }})</h2><p>{{ $article->content }}</p><form method="post" action="{{route('knowledge.assignments.store',$article)}}">@csrf<select name="core_user_id">@foreach($users as $u)<option value="{{$u->id}}">{{$u->name}}</option>@endforeach</select><input name="due_at" type="datetime-local"><label><input type="checkbox" name="required" value="1" checked> Requerido</label><button>Asignar</button></form><ul>@foreach($article->assignments as $a)<li>Usuario {{ $a->core_user_id }} · abierto: {{ $a->tracking?->opened_at }} · completado: {{ $a->tracking?->completed_at }} · confirmado: {{ $a->tracking?->confirmed_at }} <form method="post" action="{{route('knowledge.assignments.open',$a)}}">@csrf<button>Abrir</button></form><form method="post" action="{{route('knowledge.assignments.complete',$a)}}">@csrf<button>Completar</button></form><form method="post" action="{{route('knowledge.assignments.confirm',$a)}}">@csrf<button>Confirmar</button></form></li>@endforeach</ul></section>@endforeach</x-layouts.tenant>
