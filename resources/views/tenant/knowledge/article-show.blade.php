<x-layouts.tenant title="{{ $article->title }}" subtitle="@foreach($article->categories as $category){{ $category->name }}{{ !$loop->last ? ' · ' : '' }}@endforeach">
    <div class="d-flex flex-wrap justify-content-end gap-2 mb-3"><a class="btn btn-outline-secondary" href="{{ route('knowledge.read',$article) }}"><i data-lucide="monitor"></i> Vista de colaboradoras</a><a class="btn btn-primary" href="{{ route('knowledge.articles.edit',$article) }}">Editar / nueva versión</a>@if($article->status !== 'published')<form method="POST" action="{{ route('knowledge.articles.publish',$article) }}">@csrf @method('PATCH')<button class="btn btn-outline-success">Publicar borrador</button></form>@endif@if($article->status !== 'inactive')<form method="POST" action="{{ route('knowledge.articles.deactivate',$article) }}">@csrf @method('PATCH')<button class="btn btn-outline-warning">Archivar artículo</button></form>@endif@unless($inUse)<form method="POST" action="{{ route('knowledge.articles.destroy',$article) }}" onsubmit="return confirm('¿Eliminar este artículo?')">@csrf @method('DELETE')<button class="btn btn-outline-danger">Eliminar</button></form>@endunless</div>
    <div class="row g-3"><div class="col-lg-8"><article class="tr-card"><div class="mb-3"><span class="badge badge-soft-{{ $article->status === 'published' ? 'success' : ($article->status === 'draft' ? 'primary' : 'warning') }}">{{ $article->status === 'published' ? 'Publicado' : ($article->status === 'draft' ? 'Borrador' : 'Archivado') }}</span></div><div class="article-reading">{!! nl2br(e($article->content)) !!}</div>@if($inUse)<div class="alert alert-info mt-4 mb-0">Este artículo tiene asignaciones o historial de lectura. Solo puede archivarse.</div>@endif</article></div><div class="col-lg-4"><div class="tr-card"><h5 class="mb-3">Historial de versiones</h5><div class="list-group list-group-flush">@forelse($article->versions as $version)<div class="list-group-item px-0"><div class="d-flex justify-content-between"><strong>v{{ $version->version_number }}</strong><span class="badge badge-soft-{{ $version->status === 'published' ? 'success' : ($version->status === 'draft' ? 'primary' : 'secondary') }}">{{ $version->status === 'published' ? 'Publicada' : ($version->status === 'draft' ? 'Borrador' : 'Archivada') }}</span></div><small class="text-muted">{{ $version->published_at?->format('d/m/Y H:i') ?? $version->created_at->format('d/m/Y H:i') }} · {{ $version->readings_count }} lecturas</small>@if($version->readings->isNotEmpty())<div class="mt-2">@foreach($version->readings as $reading)<div class="small">{{ $readerNames[$reading->core_user_id] ?? 'Usuario eliminado' }} · {{ $reading->confirmed_at ? 'Confirmada' : 'Abierta' }}</div>@endforeach</div>@endif</div>@empty<div class="text-muted">No hay versiones todavía.</div>@endforelse</div></div></div></div>
</x-layouts.tenant>
