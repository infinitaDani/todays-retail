@php($editing = isset($article)); @php($version = $editableVersion ?? null); @php($selectedCategories = old('category_ids', $editing ? $article->categories->pluck('id')->all() : [])); @php($selectedAudience = old('audience', $version?->audience ?? ['all']))
<x-layouts.tenant title="{{ $editing ? 'Editar artículo' : 'Nuevo artículo' }}" subtitle="{{ $editing && $article->status === 'published' ? 'Se editará como nueva versión en borrador.' : 'Knowledge Center' }}"><div class="tr-card"><form method="POST" action="{{ $editing ? route('knowledge.articles.update',$article) : route('knowledge.articles.store') }}">@csrf @if($editing) @method('PUT') @endif<div class="row g-3"><div class="col-md-8"><label class="form-label">Título</label><input class="form-control @error('title') is-invalid @enderror" name="title" value="{{ old('title',$version?->title ?? '') }}" required>@error('title')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="col-md-4"><label class="form-label">Categorías</label><select class="form-select" name="category_ids[]" multiple size="3">@foreach($categories as $category)<option value="{{ $category->id }}" @selected(in_array($category->id, $selectedCategories))>{{ $category->name }}</option>@endforeach</select><div class="form-text">Puedes seleccionar varias categorías.</div></div><div class="col-md-6"><label class="form-label d-block">Audiencia</label>@foreach(['all'=>'Todas','management'=>'Management','store_admin'=>'Administrador de tienda','advisor'=>'Asesora'] as $code=>$label)<div class="form-check form-check-inline"><input class="form-check-input" type="checkbox" name="audience[]" value="{{ $code }}" id="audience-{{ $code }}" @checked(in_array($code,$selectedAudience))><label class="form-check-label" for="audience-{{ $code }}">{{ $label }}</label></div>@endforeach</div><div class="col-md-6 d-flex align-items-end"><div class="form-check form-switch mb-2"><input class="form-check-input" type="checkbox" value="1" name="requires_confirmation" id="requires_confirmation" @checked(old('requires_confirmation',$version?->requires_confirmation))><label class="form-check-label" for="requires_confirmation">Requiere confirmación de lectura</label></div></div><div class="col-12"><label class="form-label">Contenido</label><textarea class="form-control @error('content') is-invalid @enderror" rows="14" name="content" required>{{ old('content',$version?->content ?? '') }}</textarea>@error('content')<div class="invalid-feedback">{{ $message }}</div>@enderror</div></div><div class="d-flex gap-2 mt-4"><button class="btn btn-primary">Guardar borrador</button><a class="btn btn-light" href="{{ $editing ? route('knowledge.articles.show',$article) : route('knowledge.articles') }}">Cancelar</a></div></form></div></x-layouts.tenant>
