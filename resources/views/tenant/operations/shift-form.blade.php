@php($editing = isset($shift))
<x-layouts.tenant title="{{ $editing ? 'Editar turno' : 'Nuevo turno' }}" subtitle="Jornada operativa"><div class="tr-card"><form method="POST" action="{{ $editing ? route('operations.shifts.update',$shift) : route('operations.shifts.store') }}">@csrf @if($editing) @method('PUT') @endif<div class="row g-3"><div class="col-md-4"><label class="form-label">Nombre</label><input class="form-control" name="name" value="{{ old('name',$shift->name ?? '') }}" required></div><div class="col-md-4"><label class="form-label">Hora inicio</label><input class="form-control" type="time" name="start_time" value="{{ old('start_time',isset($shift) ? substr((string)$shift->start_time,0,5) : '') }}" required></div><div class="col-md-4"><label class="form-label">Hora fin</label><input class="form-control" type="time" name="end_time" value="{{ old('end_time',isset($shift) ? substr((string)$shift->end_time,0,5) : '') }}" required></div><div class="col-md-4"><label class="form-label">Estado</label><select class="form-select" name="status"><option value="active" @selected(old('status',$shift->status ?? 'active') === 'active')>Activo</option><option value="inactive" @selected(old('status',$shift->status ?? '') === 'inactive')>Inactivo</option></select></div></div><div class="d-flex gap-2 mt-4"><button class="btn btn-primary">Guardar turno</button><a class="btn btn-light" href="{{ $editing ? route('operations.shifts.show',$shift) : route('operations.shifts') }}">Cancelar</a></div></form></div></x-layouts.tenant>
