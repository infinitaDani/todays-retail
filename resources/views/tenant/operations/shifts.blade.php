<x-layouts.tenant title="Turnos" subtitle="Definiciones de jornadas operativas">
    <div class="row g-3 mb-4">@foreach ([['Total', $summary['total'], 'clock-3', 'primary'], ['Activos', $summary['active'], 'check-circle-2', 'success'], ['Inactivos', $summary['inactive'], 'pause-circle', 'warning'], ['Con asignaciones', $summary['assigned'], 'calendar-days', 'primary']] as [$label, $value, $icon, $color])<div class="col-6 col-xl-3"><div class="summary-card"><span class="summary-icon bg-{{ $color }}-subtle text-{{ $color }}"><i data-lucide="{{ $icon }}"></i></span><div class="summary-value">{{ $value }}</div><div class="summary-label">{{ $label }}</div></div></div>@endforeach</div>
    <div class="tr-card p-0 overflow-hidden"><form class="listing-toolbar p-3 border-bottom" method="GET"><div class="listing-search input-group"><span class="input-group-text bg-transparent"><i data-lucide="search"></i></span><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Buscar turno"></div><div class="listing-filters"><select class="form-select" name="status" onchange="this.form.submit()"><option value="">Todos los estados</option><option value="active" @selected(request('status') === 'active')>Activos</option><option value="inactive" @selected(request('status') === 'inactive')>Inactivos</option></select><a class="btn btn-outline-secondary" href="{{ route('operations.shifts') }}">Limpiar</a><a class="btn btn-primary" href="{{ route('operations.shifts.create') }}"><i data-lucide="plus" class="me-1"></i>Nuevo turno</a></div></form><div class="table-responsive"><table class="table table-custom align-middle mb-0"><thead><tr><th>Turno</th><th>Horario</th><th>Checklists</th><th>Asignaciones</th><th>Estado</th><th class="text-end">Acciones</th></tr></thead><tbody>@forelse($shifts as $shift)<tr><td class="fw-semibold">{{ $shift->name }}</td><td>{{ \Illuminate\Support\Carbon::parse($shift->start_time)->format('H:i') }} – {{ \Illuminate\Support\Carbon::parse($shift->end_time)->format('H:i') }}</td><td>{{ $shift->checklists_count }}</td><td>{{ $shift->assignments_count }}</td><td><span class="badge badge-label badge-soft-{{ $shift->status === 'active' ? 'success' : 'warning' }}">{{ $shift->status === 'active' ? 'Activo' : 'Inactivo' }}</span></td><td class="text-end"><a class="btn btn-sm btn-light" href="{{ route('operations.shifts.show',$shift) }}"><i data-lucide="eye"></i></a><a class="btn btn-sm btn-light" href="{{ route('operations.shifts.edit',$shift) }}"><i data-lucide="pencil"></i></a></td></tr>@empty<tr><td colspan="6"><div class="listing-empty">No se encontraron turnos.</div></td></tr>@endforelse</tbody></table></div><div class="listing-pagination px-3">{{ $shifts->links() }}</div></div>
</x-layouts.tenant>
