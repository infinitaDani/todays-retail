<x-layouts.tenant><h1>Calendario {{ $month }}</h1><form method="get"><input type="month" name="month" value="{{ $month }}"><button>Ir</button></form><form method="post" action="{{route('operations.assignments.store')}}">@csrf<input type="date" name="date" required><select name="core_user_id">@foreach($users as $u)<option value="{{$u->id}}">{{$u->name}}</option>@endforeach</select><select name="branch_id">@foreach($branches as $b)<option value="{{$b->id}}">{{$b->name}}</option>@endforeach</select><select name="shift_id">@foreach($shifts as $s)<option value="{{$s->id}}">{{$s->name}}</option>@endforeach</select><button>Asignar</button></form><ul>@foreach($assignments as $a)<li>{{ $a->date->format('Y-m-d') }} · {{ $a->core_user_id }} · {{ $a->branch->name }} · {{ $a->shift->name }} <form method="post" action="{{route('operations.assignments.destroy',$a)}}">@csrf @method('delete')<button>Eliminar</button></form></li>@endforeach</ul></x-layouts.tenant>
