<x-layouts.tenant title="Solicitudes" subtitle="Vacaciones, permisos y suministros">
    <div class="d-flex justify-content-end mb-3"><a class="btn btn-primary" href="{{ route('requests.create') }}">Nueva solicitud</a></div>
    <div class="tr-card"><div class="table-responsive"><table class="table table-custom"><thead><tr><th>Tipo</th><th>Estado</th><th>Periodo</th><th></th></tr></thead><tbody>@forelse ($requests as $tenantRequest)<tr><td>{{ ['vacation' => 'Vacaciones', 'permission' => 'Permiso', 'supply' => 'Suministros'][$tenantRequest->type] }}</td><td>{{ ['pending' => 'Pendiente', 'approved' => 'Aprobada', 'rejected' => 'Rechazada'][$tenantRequest->status] }}</td><td>{{ $tenantRequest->month_key ?? $tenantRequest->starts_at?->format('d/m/Y') ?? '—' }}</td><td><a class="btn btn-sm btn-light" href="{{ route('requests.show', $tenantRequest) }}">Ver</a></td></tr>@empty<tr><td colspan="4" class="text-muted">No hay solicitudes.</td></tr>@endforelse</tbody></table></div>{{ $requests->links() }}</div>
</x-layouts.tenant>
