<x-layouts.tenant title="{{ $product->name }}" subtitle="{{ $product->catalog_code }}"><div class="d-flex justify-content-end gap-2 mb-3"><a class="btn btn-primary" href="{{ route('products.edit',$product) }}">Editar</a><form method="POST" action="{{ route('products.status',$product) }}">@csrf @method('PATCH')<button class="btn btn-outline-warning">{{ $product->is_active?'Desactivar':'Activar' }}</button></form></div><div class="tr-card mb-3"><dl class="row mb-0"><dt class="col-md-3">Categoría</dt><dd class="col-md-9">{{ $product->category?->name?:'—' }}</dd><dt class="col-md-3">Colección</dt><dd class="col-md-9">{{ $product->collection?->name?:'—' }}</dd><dt class="col-md-3">Línea</dt><dd class="col-md-9">{{ $product->line?->name?:'—' }}</dd></dl></div><div class="tr-card"><h5>Variantes</h5><table class="table table-custom"><thead><tr><th>SKU</th><th>Atributos</th><th>Stock</th><th>Precio</th><th>Estado</th></tr></thead><tbody>@forelse($product->variants as $v)<tr><td>{{ $v->sku }}</td><td>@foreach($v->attributeValues as $value)<span class="badge badge-soft-primary">{{ $value->attribute->name }}: {{ $value->value }}</span>@endforeach</td><td>{{ $v->stock }}</td><td>{{ $v->sale_price }}</td><td>{{ $v->is_active?'Activa':'Inactiva' }}</td></tr>@empty<tr><td colspan="5">Sin variantes.</td></tr>@endforelse</tbody></table></div></x-layouts.tenant>
